import * as types from '../actions/action-types'


/* Not required yet */ 


const initialState = {}

const coffeeMachineReducer = (state = initialState, action) => {
	return state
}

export default coffeeMachineReducer
